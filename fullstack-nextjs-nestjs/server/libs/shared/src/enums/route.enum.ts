export enum RouteStatus {
  COMPLETE = 'complete',
  OVER = 'over',
  OUT = 'out',
  BAD = 'bad',
  DROP = 'drop',
}

export enum RouteType {
  IN = 'IN',
  OUT = 'OUT',
}

export enum SurveyType {
  LIST = 'LIST',
  PANEL = 'PANEL',
}
